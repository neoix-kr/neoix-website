name = "neoix-website"
main = "worker.js"
compatibility_date = "2025-06-01"

# 정적 사이트 + 라우팅 워커 — 레포 루트의 파일을 정적 자산으로 서빙,
# 파일이 없는 경로만 worker.js가 처리(/link/{slug} 등)
[assets]
directory = "./"
binding = "ASSETS"
not_found_handling = "404-page"
