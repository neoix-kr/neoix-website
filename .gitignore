songsan/assets/boat-btn.png

# Apple push key (secret)
AuthKey_*.p8
*.p8
neoix-push/.wrangler/

# Firebase (secret — 커밋 금지)
google-services.json
**/google-services.json
*firebase-adminsdk*.json

# 로컬 전용 설정 — 토큰 포함 가능, 절대 커밋 금지
.claude/settings.local.json
